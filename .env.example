# Configuration OpenRefine Docker
# Copiez ce fichier vers .env pour personnaliser les paramètres

# Port d'accès à OpenRefine (par défaut: 8888)
OPENREFINE_PORT=8888

# Mémoire allouée à Java (par défaut: 2048M)
OPENREFINE_MEMORY=2048M

# Interface d'écoute (par défaut: 0.0.0.0 pour toutes les interfaces)
OPENREFINE_HOST=0.0.0.0

# Nom du conteneur (par défaut: openrefine)
CONTAINER_NAME=openrefine

# Nom du volume pour les données (par défaut: openrefine_data)
VOLUME_NAME=openrefine_data
